const dansPlanet = (updatePlanet) => {
    updatePlanet({
        positionX: 400,
        positionY: 300,
        radius: 30,
        colour: 'maroon'
    })
}

export default {
    'Dan': dansPlanet
}